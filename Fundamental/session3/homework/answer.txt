- What is nested list?
Answer: A nested list is a list that appears as an element in another list.

- Can a list store both integers and strings in it?
Answer: A list can store both integers and strings in it